@model IEnumerable<DigitalUpskills.Models.tbl_Slide>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tbl_Course.Course_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Slide_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Slide_Detail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_Course.Course_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Slide_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Slide_Detail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Slide_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Slide_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Slide_Id })
        </td>
    </tr>
}

</table>
